import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 'auto',
    height: 40,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    borderColor: 'black',
    marginTop: 10,
    marginBottom: 10,
  },
  text: {
    fontSize: 26,
    fontWeight: '800',
  },
});
