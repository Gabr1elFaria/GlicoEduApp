import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    borderRadius: 6,
    width: 120,
    gap: 10,
    height: 50,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 2,
  },
  text: {
    fontSize: 24,
    fontWeight: '600',
    color: '#111111',
  },
});
