import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  text: {
    marginTop: -10,
    fontSize: 22,
    fontWeight: '500',
  },
  textBox: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
    borderBottomWidth: 1,
  },
  box: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'center',
  },
  icon: {
    display: 'flex',
    width: 40,
    height: 40,
  },
});
