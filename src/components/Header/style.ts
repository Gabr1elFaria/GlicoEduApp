import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  container: {
    height: 64,
    width: 'auto',
    marginTop: 34,
    paddingLeft: 16,
    paddingRight: 16,
    display: 'flex',
    backgroundColor: '#fff',
    justifyContent: 'space-between',
    alignItems: 'center',
    flexDirection: 'row',
  },
});
